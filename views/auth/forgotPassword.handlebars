<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>marketSpace | Recover Password </title>
</head>

<body>
    <main class="index-main">
        {{> _navbar}}
        <section class="main-content">
            <main class=" section-auth reset">
                <div class="box__login reset-card">

                    <form action="/auth/forgotPassword" method="POST" class="form section-contact__form">
                        {{> _alerts}}
                        <h3 class="u-heading--secondary u-margin-bottom--small reset-heading">
                            <i class="icon-ecommerce-bag"></i> Recover Password
                        </h3>

                        <div class="form__group">
                            <input class="form__input" type="email" placeholder="Email" id="email" name="email"
                                required />
                            <label class="form__label" for="email">Email</label>
                        </div>

                        <button type="submit" class="btn btn-primary btn--block u-margin-bottom--small">
                            <i class="icon-ecommerce-bag"></i> RECOVER PASSWORD
                        </button>
                        <p class="u-center-text">
                            Don't have an account?
                            <a href="/auth">Register</a>
                        </p>
                    </form>
                </div>
            </main>
        </section>
    </main>

    <script src="../js/lightbox-plus-jquery.js"></script>
</body>

</html>